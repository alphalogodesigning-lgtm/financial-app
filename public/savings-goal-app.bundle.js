// Generated by scripts/build-legacy-pages.mjs. Do not edit directly.
const { useEffect, useMemo, useState } = React;
const { loadBudgetData, saveBudgetData, getInitialData, dateTime } = window.AppShared;
const EXAMPLE_GOAL = { name: 'Dream Phone', targetAmount: 5999 };
const DOPAMINE_LINES = [
    'Momentum check: {pct}% closer than yesterday-you.',
    'You are {pct}% closer to this goal.',
    'Tiny steps, big flex — {pct}% complete.',
    'Progress hit: {pct}% done, keep stacking.',
    'Every ringgit counts. You are {pct}% in.',
    '{pct}% conquered. Stay locked in.',
    'Goal energy is real: {pct}% complete.',
    'Discipline meter: {pct}% and climbing.',
    '{pct}% closer to the version of you that planned this.',
    'You built {pct}% of this win already.',
    'Savings streak vibe: {pct}% complete.',
    '{pct}% down. Keep the pressure on.',
    'You are {pct}% into this mission.',
    'This goal is now {pct}% yours.',
    'You pushed this goal to {pct}% complete.'
];
const formatCurrency = (value) => `RM${Number(value || 0).toLocaleString('en-MY', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
const toAmount = (value) => {
    const parsed = Number(value);
    return Number.isFinite(parsed) && parsed > 0 ? parsed : 0;
};
const getDopamineLine = (goal, progress) => {
    const index = Math.abs(Number(goal.id) || 0) % DOPAMINE_LINES.length;
    return DOPAMINE_LINES[index].replace('{pct}', progress.toFixed(1));
};
function SavingsGoalPage() {
    const [data, setData] = useState(getInitialData('savings-goal'));
    const [isHydrated, setIsHydrated] = useState(false);
    const [goalName, setGoalName] = useState('');
    const [goalTarget, setGoalTarget] = useState('');
    useEffect(() => {
        let active = true;
        loadBudgetData().then((saved) => {
            if (!active)
                return;
            if (saved) {
                setData((prev) => ({
                    ...prev,
                    ...saved,
                    savingsGoals: saved.savingsGoals || [],
                    linkedSimulatorGoalId: saved.linkedSimulatorGoalId || null
                }));
            }
            setIsHydrated(true);
        });
        return () => { active = false; };
    }, []);
    const goals = data.savingsGoals || [];
    const linkedSimulatorGoalId = data.linkedSimulatorGoalId || null;
    const totals = useMemo(() => {
        const target = goals.reduce((sum, goal) => sum + (Number(goal.targetAmount) || 0), 0);
        const saved = goals.reduce((sum, goal) => sum + (Number(goal.currentAmount) || 0), 0);
        const remaining = Math.max(0, target - saved);
        const progress = target > 0 ? (saved / target) * 100 : 0;
        return { target, saved, remaining, progress };
    }, [goals]);
    const persist = (nextGoals, nextLinkedSimulatorGoalId = linkedSimulatorGoalId) => {
        const linkedStillValid = nextGoals.some((goal) => goal.id === nextLinkedSimulatorGoalId);
        const next = {
            ...data,
            savingsGoals: nextGoals,
            linkedSimulatorGoalId: linkedStillValid ? nextLinkedSimulatorGoalId : null
        };
        setData(next);
        saveBudgetData(next);
    };
    const createGoal = (event) => {
        event.preventDefault();
        const name = goalName.trim();
        const targetAmount = toAmount(goalTarget);
        if (!name || !targetAmount)
            return;
        const now = new Date().toISOString();
        const nextGoals = [
            {
                id: Date.now(),
                name,
                targetAmount,
                currentAmount: 0,
                createdAt: now,
                updatedAt: now,
                transactions: []
            },
            ...goals
        ];
        persist(nextGoals);
        setGoalName('');
        setGoalTarget('');
    };
    const createExampleGoal = () => {
        if (goals.some((goal) => goal.name.toLowerCase() === EXAMPLE_GOAL.name.toLowerCase()))
            return;
        const now = new Date().toISOString();
        persist([
            {
                id: Date.now(),
                name: EXAMPLE_GOAL.name,
                targetAmount: EXAMPLE_GOAL.targetAmount,
                currentAmount: 0,
                createdAt: now,
                updatedAt: now,
                transactions: []
            },
            ...goals
        ]);
    };
    const delayGoal = (goalId) => {
        const goalToDelay = goals.find((goal) => goal.id === goalId);
        if (!goalToDelay)
            return;
        const confirmed = window.confirm(`Are you sure you want to delay "${goalToDelay.name}"?`);
        if (!confirmed)
            return;
        persist(goals.filter((goal) => goal.id !== goalId));
    };
    const linkGoalToSimulator = (goalId) => {
        persist(goals, goalId);
        window.alert('Goal linked to Purchase Simulator.');
    };
    const updateGoalBalance = (goalId, type) => {
        const amountRaw = window.prompt(type === 'add' ? 'How much do you want to add? (RM)' : 'How much do you need to deduct? (RM)');
        if (amountRaw === null)
            return;
        const amount = toAmount(amountRaw);
        if (!amount) {
            window.alert('Please enter a valid amount greater than 0.');
            return;
        }
        const nextGoals = goals.map((goal) => {
            if (goal.id !== goalId)
                return goal;
            const currentAmount = Number(goal.currentAmount) || 0;
            if (type === 'deduct' && amount > currentAmount) {
                window.alert('Deduction exceeds current savings for this goal.');
                return goal;
            }
            const updatedAmount = type === 'add' ? currentAmount + amount : currentAmount - amount;
            const stamp = dateTime.nowUtcISOString();
            const transactions = [
                {
                    id: `${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
                    type,
                    amount,
                    createdAt: stamp
                },
                ...(goal.transactions || [])
            ].slice(0, 6);
            return {
                ...goal,
                currentAmount: updatedAmount,
                updatedAt: stamp,
                transactions
            };
        });
        persist(nextGoals);
    };
    if (!isHydrated) {
        return (React.createElement("div", { className: "container" },
            React.createElement("div", { className: "empty-state" }, "Loading savings goals...")));
    }
    return (React.createElement("div", { className: "container" },
        React.createElement("nav", { className: "main-nav" },
            React.createElement("a", { href: "index.html", className: "nav-link" }, "\uD83D\uDCCA Dashboard"),
            React.createElement("a", { href: "fixed-expenses.html", className: "nav-link" }, "\u2693 Fixed Expenses"),
            React.createElement("a", { href: "variable-spending.html", className: "nav-link" }, "\uD83D\uDCB8 Variable Spending"),
            React.createElement("a", { href: "savings-goal.html", className: "nav-link active" }, "\uD83C\uDFAF Savings Goal"),
            React.createElement("a", { href: "projections.html", className: "nav-link" }, "\uD83D\uDD2E Projections"),
            React.createElement("a", { href: "purchase-simulator.html", className: "nav-link" }, "\uD83E\uDDEA Simulator"),
            React.createElement("a", { href: "insights.html", className: "nav-link" }, "\uD83E\uDDE0 Insights")),
        React.createElement("header", { className: "page-header" },
            React.createElement("h1", null, "Savings Goals"),
            React.createElement("p", null, "Create clear targets, add or deduct funds anytime, and track exactly how close you are to each goal.")),
        React.createElement("section", { className: "card", style: { marginBottom: '16px' } },
            React.createElement("h2", null, "Overall Progress"),
            React.createElement("div", { className: "progress-wrap" },
                React.createElement("div", { className: "progress-track" },
                    React.createElement("div", { className: "progress-fill", style: { width: `${Math.min(100, totals.progress)}%` } }))),
            React.createElement("div", { className: "metric-grid" },
                React.createElement("div", { className: "metric" },
                    React.createElement("div", { className: "metric-label" }, "Total Target"),
                    React.createElement("div", { className: "metric-value" }, formatCurrency(totals.target))),
                React.createElement("div", { className: "metric" },
                    React.createElement("div", { className: "metric-label" }, "Saved"),
                    React.createElement("div", { className: "metric-value" }, formatCurrency(totals.saved))),
                React.createElement("div", { className: "metric" },
                    React.createElement("div", { className: "metric-label" }, "Remaining"),
                    React.createElement("div", { className: "metric-value" }, formatCurrency(totals.remaining))))),
        React.createElement("div", { className: "layout" },
            React.createElement("section", { className: "card" },
                React.createElement("h2", null, "Create a Goal"),
                React.createElement("form", { className: "form-grid", onSubmit: createGoal },
                    React.createElement("label", { htmlFor: "goal-name" }, "Goal Name"),
                    React.createElement("input", { id: "goal-name", value: goalName, onChange: (e) => setGoalName(e.target.value), placeholder: "Buying my dream phone" }),
                    React.createElement("label", { htmlFor: "goal-target" }, "Target Amount (RM)"),
                    React.createElement("input", { id: "goal-target", type: "number", min: "1", step: "0.01", value: goalTarget, onChange: (e) => setGoalTarget(e.target.value), placeholder: "5999" }),
                    React.createElement("button", { className: "btn", type: "submit" }, "Create Savings Goal"),
                    React.createElement("button", { className: "btn ghost", type: "button", onClick: createExampleGoal }, "+ Add Example: Dream Phone (RM5,999)")),
                React.createElement("p", { className: "helper" }, "Tip: Use + when adding money and - when emergencies force you to use part of your savings.")),
            React.createElement("section", { className: "goals" }, goals.length === 0 ? (React.createElement("div", { className: "empty-state" }, "No savings goals yet. Create your first goal to start building momentum.")) : goals.map((goal) => {
                const targetAmount = Number(goal.targetAmount) || 0;
                const currentAmount = Number(goal.currentAmount) || 0;
                const remaining = Math.max(0, targetAmount - currentAmount);
                const progress = targetAmount > 0 ? (currentAmount / targetAmount) * 100 : 0;
                const isLinkedToSimulator = linkedSimulatorGoalId === goal.id;
                return (React.createElement("article", { key: goal.id, className: "card" },
                    React.createElement("div", { className: "goal-title" },
                        React.createElement("h3", null, goal.name),
                        React.createElement("p", { className: "goal-target" },
                            "Target: ",
                            formatCurrency(targetAmount))),
                    React.createElement("p", { className: "goal-dopamine-line" }, getDopamineLine(goal, progress)),
                    React.createElement("div", { className: "progress-wrap" },
                        React.createElement("div", { className: "progress-track" },
                            React.createElement("div", { className: "progress-fill", style: { width: `${Math.min(100, progress)}%` } }))),
                    React.createElement("div", { className: "metric-grid" },
                        React.createElement("div", { className: "metric" },
                            React.createElement("div", { className: "metric-label" }, "Saved"),
                            React.createElement("div", { className: "metric-value" }, formatCurrency(currentAmount))),
                        React.createElement("div", { className: "metric" },
                            React.createElement("div", { className: "metric-label" }, "Remaining"),
                            React.createElement("div", { className: "metric-value" }, formatCurrency(remaining))),
                        React.createElement("div", { className: "metric" },
                            React.createElement("div", { className: "metric-label" }, "Progress"),
                            React.createElement("div", { className: "metric-value" },
                                progress.toFixed(1),
                                "%"))),
                    React.createElement("div", { className: "btn-row", style: { marginTop: '12px' } },
                        React.createElement("button", { type: "button", className: "btn", onClick: () => updateGoalBalance(goal.id, 'add') }, "\uFF0B Add Money"),
                        React.createElement("button", { type: "button", className: "btn ghost", onClick: () => updateGoalBalance(goal.id, 'deduct') }, "\uFF0D Deduct"),
                        React.createElement("button", { type: "button", className: `btn ghost ${isLinkedToSimulator ? 'linked-goal-btn' : ''}`, onClick: () => linkGoalToSimulator(goal.id) }, isLinkedToSimulator ? '✅ Linked to Simulator' : '🔗 Link to Simulator')),
                    React.createElement("button", { type: "button", className: "delay-goal-text", onClick: () => delayGoal(goal.id) }, "Delay this goal"),
                    (goal.transactions || []).length > 0 && (React.createElement("div", { className: "mini-history" }, (goal.transactions || []).slice(0, 3).map((txn) => (React.createElement("div", { className: "history-row", key: txn.id },
                        React.createElement("span", { className: txn.type === 'add' ? 'add' : 'deduct' },
                            React.createElement("strong", null, txn.type === 'add' ? '+' : '-'),
                            " ",
                            formatCurrency(txn.amount)),
                        React.createElement("span", null, new Date(txn.createdAt).toLocaleDateString('en-MY')))))))));
            })))));
}
ReactDOM.render(React.createElement(SavingsGoalPage, null), document.getElementById('root'));
